## 🟦 Searching Algorithms – Python Utilities

```python
from typing import List
```

---

### 1️⃣ Linear Search

```python
def linear_search(arr: List[int], target: int) -> int:
    for i, val in enumerate(arr):
        if val == target:
            return i
    return -1
```

---

### 2️⃣ Binary Search (Iterative)

> ⚠️ Array **must be sorted**

```python
def binary_search(arr: List[int], target: int) -> int:
    left, right = 0, len(arr) - 1

    while left <= right:
        mid = (left + right) // 2

        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1

    return -1
```

---

### 3️⃣ First Occurrence (Lower Bound)

```python
def first_occurrence(arr: List[int], target: int) -> int:
    left, right = 0, len(arr) - 1
    result = -1

    while left <= right:
        mid = (left + right) // 2

        if arr[mid] == target:
            result = mid
            right = mid - 1   # move left
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1

    return result
```

---

### 4️⃣ Last Occurrence (Upper Bound)

```python
def last_occurrence(arr: List[int], target: int) -> int:
    left, right = 0, len(arr) - 1
    result = -1

    while left <= right:
        mid = (left + right) // 2

        if arr[mid] == target:
            result = mid
            left = mid + 1    # move right
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1

    return result
```

---

### 5️⃣ Count Occurrences in Sorted Array

```python
def count_occurrences(arr: List[int], target: int) -> int:
    first = first_occurrence(arr, target)
    if first == -1:
        return 0

    last = last_occurrence(arr, target)
    return last - first + 1
```

---

## 🧪 Example Usage

```python
arr = [1, 2, 2, 2, 3, 4, 5]

print(linear_search(arr, 3))        # 4
print(binary_search(arr, 4))        # 5
print(first_occurrence(arr, 2))     # 1
print(last_occurrence(arr, 2))      # 3
print(count_occurrences(arr, 2))    # 3
```

---

## 🧠 Interview Cheat Sheet

| Function              | Requirement | Time     |
| --------------------- | ----------- | -------- |
| Linear search         | Any array   | O(n)     |
| Binary search         | Sorted      | O(log n) |
| First/Last occurrence | Sorted      | O(log n) |
| Count occurrences     | Sorted      | O(log n) |

---

### 🔥 Pro Binary Search Rule (memorize this)

> **Whenever you see**
> “sorted”, “first”, “last”, “minimum”, “maximum”, “boundary”
> → **Binary Search**

---
